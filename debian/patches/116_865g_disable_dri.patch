diff -Nurp patched/src/i830_driver.c working/src/i830_driver.c
--- patched/src/i830_driver.c	2009-04-04 12:48:27.000000000 -0700
+++ working/src/i830_driver.c	2009-04-04 12:48:02.000000000 -0700
@@ -1655,7 +1655,12 @@ I830AccelMethodInit(ScrnInfoPtr pScrn)
 	xf86DrvMsg(pScrn->scrnIndex, X_CONFIG, "DRI is disabled because it "
 		"runs only at depths 16 and 24.\n");
 	pI830->directRenderingType = DRI_DISABLED;
+    } else if (IS_I865G(pI830)) {
+	xf86DrvMsg(pScrn->scrnIndex, X_CONFIG, "DRI is disabled because it "
+		"fails to run without freezing on 865G chips. (See LP 304871)\n");
+	pI830->directRenderingType = DRI_DISABLED;
     }
+
 #endif /* XF86DRI */
 
     I830MapMMIO(pScrn);
